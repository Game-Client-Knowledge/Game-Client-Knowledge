# 2026腾讯游戏客户端一面

## 八股

### 讲一下C++的内存结构

### 智能指针了解吗？介绍下共享指针

### 共享指针的引用计数存在什么位置？

### 局部静态变量的初始化和内存分配发生在什么阶段？

### 类型题：

```
#define string char*

typedef char* string1 

string a,b
string1 a1,b1

a,b,a1,b1的类型
```

### 类型题

```
A{
    virtual f1()
    {
        xxx
    }
}
B : A{
    virtual f2()
    {
        xxx
    }
}

有几个虚表指针，几个虚函数表
```

### 操作系统：多线程中，对某个数进行++，我们需要加锁才能保证数据稳定。从操作系统的层面来讲，为什么不加锁会导致数据不稳定。

### 追问：从主存读取的数据存到了哪？

### 追问：读取需要++的变量时，数据最终放到了哪里？

### 追问：寄存器是每个线程独立的吗？

## 算法

### 1.给你一个数组，如果相邻的值相加为10则可以消除，比如1，1，9，9，最后全可以消除。设计一个算法，输入一个数组，输出该数组消除后的剩余元素数量

### 2.链表的倒数第K个值
